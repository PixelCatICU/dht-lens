services:
  dht-lens:
    build:
      context: .
      dockerfile: Dockerfile
    image: dht-lens:latest
    container_name: dht-lens
    network_mode: host
    restart: unless-stopped
    environment:
      NODE_ENV: production
